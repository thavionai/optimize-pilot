---
description: Show how many tokens compression would save and why (dry run, no model call)
argument-hint: <text to analyze>
---
Call the `optimize_discover` tool with the text below, then present:

1. The optimized text in a code block.
2. The estimated tokens before → after and % saved.
3. The per-rule-group breakdown (which group saves the most), verbatim.

Do **not** rewrite or summarize the text yourself — relay the tool's report.

Text to analyze:
$ARGUMENTS
