---
description: Show lifetime token savings from optimize-pilot (prompts + command output)
---
Call the `optimize_stats` tool (no arguments) and relay its summary verbatim:
lifetime prompt optimizations, command-output compressions, and the estimated
tokens saved across both.
